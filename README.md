Clone the repository:
git clone https://github.com/your-username/sales-dashboard.git
cd sales-dashboard
cd dashboard-backend
Install dependencies: interminal run : npm install
for dashboard-backend now run in terminal: node server.js
this would give the messgae that server is running at local host 5000
now open a new terminal and for write in terminal:cd dashboard-frontend     to change the path 
now interminal : npm start
this will start the project 
for TEst cases: you can select any state from the state select dropdown( by defualt : kentucky)
by selecting the state it will give appropriately the date from and to(that is min date and max date listed for it in the data) 
it also gives profit which is total of all profits in that state, total average discount, total quantity that has been sold
and total sales for that particular state 
similarly below are the charts: that shows the metrics according to the date: 
bar chart1 : sales by city in that particular state
Table : sales by product type in that particular state
pie chart: sales by category 
bar chart: sales by subcategory
sales by segment
